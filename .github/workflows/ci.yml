name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]

    env:
      DEBUG: "1"
      ENVIRONMENT: test

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"

    - name: Lint with ruff
      run: |
        ruff check app tests

    - name: Run tests with coverage
      run: |
        pytest tests/ -v --tb=short --cov=app --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=95

    - name: Verify core modules have 100% coverage
      run: |
        python scripts/check_core_coverage.py

    - name: Upload coverage
      uses: codecov/codecov-action@v3
      if: always()
      with:
        file: ./coverage.xml
        fail_ci_if_error: false
